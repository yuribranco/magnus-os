# Design: Radar de Criativos — MVP magro (100% online / server-hosted, produto standalone)

Generated by /office-hours on 2026-06-02
Repo: yuribranco/magnus-os (produto Radar = repo próprio a criar)
Status: APPROVED
Mode: Startup (intrapreneurship — produto pra vender dentro do Magnus OS)
Supersedes: complementa `2026-06-01-radar-kickoff-brief.md` (resolve a decisão de arquitetura que o brief deixou aberta)

## ⚙️ Atualização de arquitetura (2026-06-02) — DECISÃO FINAL: 100% online / server-hosted (A)

> Histórico das viradas: office-hours fechou em **A** → Yuri reabriu "por que não local?" e foi pra
> **Híbrido H** → Yuri reconsiderou e **travou em A (100% online)**. Esta é a decisão vigente. As
> seções H e a Premise #2 abaixo ficam só como registro do raciocínio.

**Decisão do Yuri (2026-06-02, final):** o **Radar é 100% online (server-hosted)**, produto
**standalone pra vender**. O **Magnus OS local PUXA os dados do Radar** (via API/sync) pra construir
criativo/brief. Toda a coleta + scoring + (opcionalmente) o brief vivem no servidor do Radar; o
cliente consome o resultado.

**Por que A e não H/L (o que mudou o voto):** o Radar deixou de ser "feature do Magnus OS" e virou
**produto de venda própria**. Como produto standalone, o valor está no **dataset central + relatório
acionável** que só o server cheio entrega (benchmark cross-cohort, "o que está vencendo no nicho",
histórico, multi-tenant). A preocupação de margem (inferência server-side) passa a ser **coberta
pelo preço da assinatura do Radar** — a `/deep-research` de pricing do mercado (rodando 2026-06-02)
ancora a faixa pra garantir que a assinatura cubra Apify + Claude + margem.

### Contrato (server-hosted, A)
| Peça | Onde roda | Quem paga | Nota |
|---|---|---|---|
| **Apify (scrape Ad Library)** | servidor Radar, **token do Yuri** | Yuri (assinatura cobre) | cliente não provisiona nada |
| **Quota por usuário** | servidor | — | teto de scrape por licença = guardrail de custo |
| **DB de pesquisas consolidadas** | servidor (Supabase) | — | cache/dedup + **dataset central** (benchmark cross-cohort = diferencial do produto) |
| **winner-score** | servidor | — | computação central |
| **brief do Claude** | servidor (chave do Yuri) OU sync pro Magnus OS local | Yuri (server) | decisão fina no /plan-eng-review; preço cobre se for server |
| **Consumo pelo Magnus OS local** | cliente puxa via API/sync | — | local usa os dados do Radar pra montar criativo |

**Implicação de margem (registrada):** A faz o Yuri pagar inferência + Apify recorrentes. Mitigação =
**pricing** (não arquitetura). Por isso a deep-research de mercado + pricing benchmark é input direto
da precificação. Alavancas se apertar: brief no Claude local do cliente (não server), quota, cache.

**Sub-decisão:** coleta via **Apify** (token do Yuri, sem auth Meta) é o default; Ad Library API
oficial = otimização de custo futura.

---

## Problem Statement
Quem roda lançamento/venda de infoproduto precisa de inteligência competitiva: que ângulos/criativos
de concorrentes estão vencendo. Hoje a cohort de mentorados do Magnus (50-200+) faz isso **na mão** —
abre a Meta Ad Library, printa, copia ângulos — e perde horas, sem método de pontuar o que de fato
está escalando. O Radar automatiza essa espionagem e a transforma em briefs de criativo PT-BR.

## Demand Evidence
**O status quo é o concorrente real, e ele existe:** os mentorados JÁ espionam concorrentes
manualmente hoje (Ad Library + prints + cópia de ângulos), gastando horas. Não é hipótese — é um
workaround custoso que já roda. (Forcing question Q1: "vejo eles fazendo isso na mão hoje".)
**Gap honesto:** ninguém pagou ainda especificamente pelo Radar nem pediu "quando lança" — a demanda
é inferida do comportamento manual, não de dinheiro na mesa. Daí a Assignment abaixo.

## Status Quo
Espionagem manual: abrir Meta Ad Library por concorrente conhecido, rolar os ads ativos, julgar "no
olho" o que parece estar vencendo (sem score), printar, e copiar ângulo. Custa horas/semana e é
enviesado (só pega o concorrente que o mentorado já conhece; não descobre os que ele não conhece).

## Target User & Narrowest Wedge
**Usuário:** mentorado do Magnus rodando ofertas de infoproduto (cohort 50-200+).
**Wedge travado (Q5):** mentorado nomeia concorrentes principais **+ o Radar auto-descobre mais**
(seed keywords → Ad Library, traz concorrentes que o mentorado nem conhecia como referência) →
puxa os ads ativos → **ranqueia por winner-score** (longevidade `daysActive` + variações
`collationCount` + velocidade; API-only, validado empírico) → **Claude gera 1 brief de ângulo PT-BR**
(lente Breakthrough Advertising, nunca cópia). **Fora do wedge:** orgânico (IG/YouTube),
monitoramento contínuo/alertas, deep-spy v2 (funil+infra+SSRF).

## Constraints
- Server-hosted: o Yuri quer **controle de ponta a ponta** e cobra **assinatura recorrente** que
  cobre Apify + Claude API. Cliente não põe chave nenhuma. (Q4: "assinatura minha cobre tudo".)
- Coleta via Apify (Meta Ad Library scraper; `APIFY_TOKEN` STARTER já existe). Engajamento de ad
  **não vem por API** → fica fora do score (v2/Playwright).
- Read-only de dado público deslogado; LGPD/postura legal pro v2 (deferido).

## Premises (confirmadas na sessão)
1. **Demanda é real mas não-monetizada ainda** — comportamento manual existe; "pagaria" é inferido.
   → a Assignment fecha esse gap antes de escalar o build. **Agree.**
2. **Arquitetura = A (server-hosted, 100% online)** — CONFIRMADA como decisão final (depois de uma
   virada A→H→A). Radar é produto standalone de venda; o dataset central + relatório justificam o
   server cheio; margem coberta via pricing. Ver "Atualização de arquitetura" no topo. **Agree.**
3. **Construir o wedge magro ANTES da plataforma** — provar willingness-to-pay com o mínimo, não
   levantar Next14+tRPC+Prisma+BullMQ+RLS multi-tenant no dia 1. **Agree (Q-abordagem: A magro).**

## ⚠️ Risco de premissa (registrado — Yuri ciente, decidiu A mesmo assim)
Server-hosted A faz o Yuri **pagar inferência Claude + Apify recorrentes** (~$7-25/tenant/mês na
estimativa). A 50-200 tenants é COGS real. **Mitigação escolhida = pricing, não arquitetura:** como o
Radar é produto standalone de venda, a assinatura precifica pra cobrir o COGS + margem (daí a
deep-research de pricing benchmark). Alavancas se apertar: mover o brief pro Claude local do cliente,
quota por licença, cache/dedup agressivo. **Decisão: A (100% online).**

## Approaches Considered
### Approach A: MVP magro (ESCOLHIDO)
Servidor mínimo que prova: Next API + Supabase + Apify + Claude API, **gated por licença**, scrape
**síncrono on-request** (sem BullMQ/filas), auto-descoberta + nomeado → winner-score → 1 brief.
**1 tenant = 1 mentorado via licença**, sem RLS multi-tenant pesado no dia 1.
Effort: M · Risk: Low · Reusa: padrão de licença do `magnus-os-licenca`, winner-score do handoff,
camada Claude (Breakthrough Adv. já no copy-chief).

### Approach B: Plataforma completa do handoff
Next14+tRPC+Prisma+Supabase RLS por tenant+BullMQ+VPS isolado, multi-tenant + monitoramento contínuo
+ provider abstrato dia 1. Effort: XL · Risk: High (semanas até o 1º pagante; constrói tudo antes de
provar). **Rejeitado por ora — é o alvo de escala, não o de validação.**

### Approach C: Concierge primeiro
Yuri roda o Radar semi-manual (Apify+Claude por script) pra 3-5 mentorados, cobra, vê se pagam.
Effort: S · Risk: Low. **Não escolhido como build, mas vira a Assignment** (validar com dinheiro).

## Recommended Approach
**A (MVP magro)**, escolhido pelo Yuri. Prova a willingness-to-pay com o mínimo viável de servidor,
preservando o caminho pra plataforma (B) quando a demanda paga estiver provada. O winner-score
API-only já está validado; a camada de licença já existe; o gargalo é a Meta (dado), não o actor.

## Open Questions (pro /plan-eng-review travar)
> Arquitetura = **A / 100% online** (travada). O que sobra é a implementação do servidor + o contrato
> de consumo pelo Magnus OS local (que puxa os dados do Radar).

1. Nome + repo (`yuribranco/radar` preferido) + local `~/Documents/Magnus/radar`.
2. VPS alvo: reusar o 187 (6 apps, "nunca pm2 restart all") vs box nova. Servidor fino de H (scrape +
   cache + quota, sem LLM) é leve → 187 provavelmente serve; confirmar.
3. Modelo de licença/tenancy: reusar o emissor do Magnus OS (Hotmart). 1 licença = 1 mentorado; a
   **quota de scrape é atrelada à licença**. Sem RLS pesado no MVP — confirmar.
4. **DB de consolidação (Supabase):** schema do cache de pesquisas. Chave de dedup = (concorrente |
   nicho/seed) + janela de frescor (quantos dias até re-scrapear?). Definir TTL e a granularidade da
   chave (por página de anunciante? por keyword?). **É o coração de H — modelar com cuidado.**
5. **Quota:** unidade (scrapes/mês? concorrentes únicos/mês?), valor default, e o que acontece no
   estouro (bloqueia? degrada pra só-cache? upsell?).
6. ~~Apify vs API oficial~~ → **Apify central, token do Yuri** (decidido). Actor: comunidade atrás de
   abstração no MVP, custom depois (config swap). API oficial = otimização de custo futura.
7. Contrato de consumo: o Radar tem **painel web próprio** (produto standalone) E expõe **API/sync
   pro Magnus OS local puxar** os dados (ads ranqueados + brief) pra montar criativo. Confirmar
   formato do JSON e auth do pull (licença).
8. Onde roda o brief do Claude: **server** (chave do Yuri, preço cobre) vs **local** (Magnus OS do
   cliente puxa ads crus e gera o brief com a sub dele = $0 inferência). Trade-off margem vs produto
   self-contained — decidir no plan-eng-review.

## Success Criteria
- Um mentorado real nomeia um concorrente, o Radar devolve ads ranqueados por winner-score + 1 brief
  de ângulo PT-BR útil, em < 1 min, sem o mentorado pôr chave nenhuma.
- A auto-descoberta traz ≥1 concorrente relevante que o mentorado NÃO tinha nomeado.
- Pelo menos 1 mentorado confirma que pagaria a assinatura (fecha o gap de demanda monetizada).

## Distribution Plan
Server-hosted (não é artefato baixável como o Magnus OS local). Deploy = VPS (PM2/Nginx) ou
plataforma de host; acesso gated por licença. A definir no /plan-eng-review (Q2/Q3).

## Dependencies
- `APIFY_TOKEN` (existe, STARTER). winner-score (handoff, validado). Emissor de licença Magnus OS
  (existe). Claude API server-side (chave do Yuri — nova dependência de custo).

## The Assignment — DISPENSADA (2026-06-02): Yuri valida por dogfood
~~Antes de levantar o servidor, conseguir 1 "sim, eu pagaria" de um mentorado.~~ **Yuri decidiu
construir de qualquer jeito e ser o próprio teste** — ele roda lançamento, então é usuário-alvo de
fato; vai usar o Radar como se fosse mentorado. **Validação = dogfood do próprio Yuri**, não
concierge externo. → Caminho direto pro build: `/plan-eng-review` do servidor online (travar as Open
Questions) → writing-plans → MVP magro. Buscar o "pagaria" de mentorado externo fica pra depois do
MVP rodar (pré-pricing/lançamento do Radar como produto).

## What I noticed about how you think
- Tu não disse "os mentorados querem" — disse "vejo eles fazendo isso na mão hoje". Isso é a melhor
  evidência de demanda que existe (comportamento, não elogio), e tu foi direto nela.
- Escolheu controle sobre margem de forma consistente: assinatura cobre tudo, cliente sem chave,
  server-hosted. Não foi indecisão — foi a mesma preferência em 3 respostas seguidas.
- Quando ofereci o wedge mínimo, tu expandiu pra incluir auto-descoberta ("traz como referência
  nossos concorrentes") — tu enxerga o diferencial onde está (achar o concorrente que o cliente não
  conhece), não só automatizar o que ele já faz.
